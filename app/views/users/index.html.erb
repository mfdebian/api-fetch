<h1>Users</h1>

<% if @users.present? %>
  <ul>
    <% @users.each do |user| %>
      <li><%= link_to user.name, user %></li>
    <% end %>
  </ul>
<% else %>
  <p>No users found.</p>
<% end %>

<br/>

<%= link_to "Add user", new_user_path %>